import React from 'react';

// Since this component is simple and static, there's no parent container for it.
const Messaging = () => {
  return (
    <div>
      <h2 className="alt-header">Welcome to your messaging inbox</h2>
    </div>
  );
};

export default Messaging;
